{
  "name": "markpad-frontend",
  "version": "0.9.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.html",
    "build": "tsc --noEmit && bun run build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "oxlint --import-plugin src tests",
    "fmt": "oxfmt src tests build.mjs index.html package.json tsconfig.json bunfig.toml .oxfmtrc.json",
    "fmt:check": "oxfmt --check src tests build.mjs index.html package.json tsconfig.json bunfig.toml .oxfmtrc.json",
    "check": "bun run typecheck && bun run test && bun run lint && bun run fmt:check"
  },
  "dependencies": {
    "dompurify": "3.4.13",
    "highlight.js": "11.11.1",
    "marked": "18.0.9"
  },
  "devDependencies": {
    "bun-plugin-tailwind": "0.1.2",
    "oxfmt": "0.63.0",
    "oxlint": "1.78.0",
    "tailwindcss": "4.3.3",
    "typescript": "7.0.2"
  }
}
